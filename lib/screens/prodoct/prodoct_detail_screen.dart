import 'package:flutter/material.dart';

class ProdoctDetailScreen extends StatelessWidget {
  final int id;
  const ProdoctDetailScreen({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
